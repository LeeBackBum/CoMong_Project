<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.sm.app.repository.UserRepository">

    <select id="selectOne" parameterType="String" resultType="edu.sm.app.dto.UserDto">
        SELECT * FROM user WHERE user_id=#{id}
    </select>

    <select id="select" resultType="edu.sm.app.dto.UserDto">
        SELECT * FROM user
    </select>

    <insert id="insert" parameterType="edu.sm.app.dto.UserDto">
        INSERT INTO user (user_id, user_pwd, user_email, user_name, user_address, user_age, user_sex, disease_status, user_number)
        VALUES (#{userId}, #{userPwd}, #{userEmail}, #{userName}, #{userAddress}, #{userAge}, #{userSex}, #{diseaseStatus}, #{userNumber})
    </insert>

    <update id="update" parameterType="edu.sm.app.dto.UserDto">
        UPDATE user
        SET user_pwd=#{userPwd},
            user_name=#{userName}
        WHERE user_id=#{userId}
    </update>

    <delete id="delete" parameterType="String">
        DELETE FROM user WHERE user_id=#{userId}
    </delete>

    <!-- 사용자 인증: 사용자명과 비밀번호로 사용자 조회 -->
    <select id="findByUsernameAndPassword" parameterType="map" resultType="edu.sm.app.dto.UserDto">
        SELECT user_id AS userId,
               user_name AS userName,
               user_pwd AS userPwd
        FROM user
        WHERE user_id = #{username}
          AND user_pwd = #{password}
    </select>

</mapper>
